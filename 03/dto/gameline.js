export default class GameLine {
    constructor(symbol, numberOfKind, winPosition, win) {
        this.symbol = symbol
        this.NumberOfKind = numberOfKind
        this.WinPosition = winPosition
        this.WinAmt = win
    }
}